do demonstrate_constraining = (x) ->
    do demonstrate_constraining(2) /* constrains x to Num here */
    
    /* will fail below because x is constrained to Num above and cannot be a 
     * list of lists of strings */
    do fail = ["I'm going to break things"] :: x
    return "wow!"
